<h1>Category: <%= @category.title %></h1>

<% @category.contests.each do |contest| %>
<h3><%= link_to contest.title, contest %></h2>
  <ul>
    <li>Bounty: $<%= contest.price %></li>
    <li>Posted by: <%= User.find(contest.creator_id).username %></li>
  </ul>
<% end %>
